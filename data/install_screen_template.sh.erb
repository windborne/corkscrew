#!/bin/bash

set -e

( crontab -l | grep -v -F "@reboot <%= @config.start_screen_command %>" ; echo "@reboot <%= @config.start_screen_command %>   # generated by corkscrew" ) | crontab -

echo "Installation complete!"
