name: Secret scan runner routing test

on:
  push:
    branches:
      - codex/test-secret-scan-routing

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  scan:
    uses: windborne/.github/.github/workflows/secret-scan.yml@codex/migrate-secret-scanner
    with:
      scan_depth: 1
